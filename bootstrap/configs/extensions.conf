[general]
static=yes
writeprotect=no

[internal]
exten => 1001,1,Dial(SIP/1001,20)
exten => 1002,1,Dial(SIP/1002,20)

exten => 999,1,Answer()
 same => n,Set(TIMEOUT(digit)=2)
 same => n,Set(TIMEOUT(response)=5)
 same => n,Background(custom/welcome)
 same => n,WaitExten(20)
 same => n,Hangup()

exten => 1,1,Playback(custom/smile)
 same => n,Hangup()